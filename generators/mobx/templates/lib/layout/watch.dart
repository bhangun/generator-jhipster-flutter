import 'package:flutter/material.dart';

class WatchLayout extends StatelessWidget {
  const WatchLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(''),
    );
  }
}